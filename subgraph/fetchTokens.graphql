query fetchTokens {
  tokens(where: { isListed: true }) {
    decimals
    isListed
    isNativeAssetSuperToken
    isSuperToken
    name
    symbol
    id
    underlyingToken {
      id
      decimals
      name
      symbol
    }
  }
}
